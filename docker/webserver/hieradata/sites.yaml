sites:
    wordpress:
        install_dir: /var/www/wordpress
        create_db: false
        db_name: council
        db_user: council
        db_password: council
        wp_owner: www-data
        wp_group: www-data
        wp_site_domain: 'localhost'
